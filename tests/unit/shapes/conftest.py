import pytest

# Currently enabled for all unit tests at top level

# @fixture(autouse=True)
# def dummmy_shaders_for_modules() -> str:
#     return "pyglet.shapes"

# @pytest.fixture(autouse=True)
# def gl_automock_config():
#     return {}